global.phase = 0


// Número máximo de obstáculos que podem estar na tela ao mesmo tempo
max_obstacles = 10;

// Tempo entre cada spawn (60 ticks = 1 segundo)
spawn_interval = 60;

// Alarme para gerar um novo obstáculo
alarm[0] = spawn_interval;
